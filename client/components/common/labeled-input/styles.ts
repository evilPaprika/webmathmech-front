import { makeStyles } from '@material-ui/core/styles';


export const useStyles = makeStyles({
    inputField: {
        display: 'flex',
        alignItems: 'baseline',

        marginBottom: '20px',
    },
    inputLabel: {
        width: '100px'
    },
});
