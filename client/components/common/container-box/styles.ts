import { makeStyles } from '@material-ui/core/styles';


export const useStyles = makeStyles(() => ({
    gap_none: {
        marginBottom: 0
    },

    gap_small: {
        marginBottom: '10px',
    },

    gap_medium: {
        marginBottom: '20px',
    },

    gap_large: {
        marginBottom: '40px',
    },
}));
